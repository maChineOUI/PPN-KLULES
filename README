# LULESH 2.0 — 现代化重构版（Kokkos 5 / OpenMP + CUDA）

LULESH（Livermore Unstructured Lagrangian Explicit Shock Hydrodynamics）是由
Lawrence Livermore National Laboratory 开发的 HPC 代理应用，用于评估处理器和编程
模型在拉格朗日流体力学计算上的性能。

本版本在 LULESH 2.0 基础上进行了系统性现代化重构，以 Kokkos 5 替换原始 OpenMP
pragma，并完成了面向 GPU 的全面迁移——所有核函数均使用 `KOKKOS_LAMBDA` 和
`Kokkos::View`，可在 OpenMP（CPU）和 CUDA（NVIDIA GPU）两个后端上编译运行。

原始项目：https://codesign.llnl.gov/lulesh.php

---

## 与原版的主要差异

| 项目 | 原版 LULESH 2.0 | 本版本 |
|------|----------------|--------|
| 构建系统 | Makefile | CMake 3.16+ |
| 并行模型 | OpenMP pragma / MPI+OpenMP | Kokkos 5（OpenMP + CUDA 后端） |
| GPU 支持 | 无 | 全核函数 `KOKKOS_LAMBDA`，可编译为 CUDA |
| MPI | 支持 | 已移除 |
| 可视化 | Silo/VisIt（可选） | 已移除 |
| 代码组织 | 4 个大文件（~2800 行） | 11 个单职责模块 |
| C++ 标准 | C++11 | C++20 |
| 内核同步 | ~94 barriers/step | ~67 barriers/step（融合后） |

---

## 项目结构

```
LULESH/
├── CMakeLists.txt
├── README
├── apps/
│   └── lulesh_main.cc        # main()、Kokkos init/finalize、主循环
├── include/                  # 公共头文件（模块接口）
│   ├── lulesh.h              # Domain 类、公共类型、Kokkos::View 成员
│   ├── lulesh-geometry.h     # 几何辅助函数（全部 KOKKOS_INLINE_FUNCTION，定义内联于此）
│   ├── lulesh-stress.h
│   ├── lulesh-nodal.h
│   ├── lulesh-kinematics.h
│   ├── lulesh-viscosity.h
│   ├── lulesh-eos.h
│   ├── lulesh-timestep.h
│   ├── lulesh-integration.h
│   ├── lulesh-init.h
│   └── lulesh-util.h
├── src/                      # 实现文件
│   ├── lulesh-stress.cc
│   ├── lulesh-nodal.cc
│   ├── lulesh-kinematics.cc
│   ├── lulesh-viscosity.cc
│   ├── lulesh-eos.cc
│   ├── lulesh-timestep.cc
│   ├── lulesh-integration.cc
│   ├── lulesh-init.cc
│   └── lulesh-util.cc
├── scripts/
│   ├── build-openmp.sh       # 一键构建（macOS / Linux，OpenMP 后端）
│   ├── build-cuda.sh         # Linux + NVIDIA GPU CUDA 后端完整构建（含 Kokkos 重建）
│   └── run-openmp.sh         # 正确性验证 / 性能基准 / 扩展性扫描
├── reports/                  # 周报（W10~W13）
├── samples/                  # 采样分析输出示例
├── tests/
│   └── baselines/            # 正确性基准参考值
└── build/                    # 构建产物（gitignore，cmake 生成）
```

---

## 模块说明

| 模块 | 功能 |
|------|------|
| `lulesh-geometry` | 形函数、体积 / 面积 / 特征长度（所有函数以 `KOKKOS_INLINE_FUNCTION` 定义在头文件中，无对应 `.cc`） |
| `lulesh-stress` | 应力积分、沙漏（Flanagan-Belytschko）控制，scatter/gather 统一路径 |
| `lulesh-nodal` | 力→加速度→速度→位置（速度与位移更新已融合为单一内核） |
| `lulesh-kinematics` | 速度梯度 + 变形率张量（已与 Lagrange 步骤融合，内联节点收集） |
| `lulesh-viscosity` | 单调人工黏性 Q 计算（单一全元素内核，已消除 per-region 循环） |
| `lulesh-eos` | 物态方程（理想气体）、材料属性更新、声速计算（11 个子核已融合） |
| `lulesh-timestep` | 时间步长控制（Courant 与水力学约束，双 reduce 单次归约） |
| `lulesh-integration` | Leap-Frog 时间积分主循环 |
| `lulesh-init` | 网格初始化、Domain 构造、EOS 临时缓冲区预分配 |
| `lulesh-util` | 命令行解析、结果验证与输出 |

---

## 依赖

| 工具 | 最低版本 | 说明 |
|------|----------|------|
| CMake | 3.16 | 构建系统 |
| C++ 编译器 | Clang 13+ / GCC 11+ | 需支持 C++20 |
| Kokkos | 5.0.2 | 并行抽象层（OpenMP 或 CUDA 后端） |
| OpenMP | 4.5+ | CPU 线程并行运行时 |
| CUDA（可选） | 11.0+ | GPU 后端，需配合 `nvcc_wrapper` 且 Kokkos 须以 `-DKokkos_ENABLE_CUDA_LAMBDA=ON` 构建 |

---

## 构建

### 快速构建（推荐，OpenMP 后端）

```bash
./scripts/build-openmp.sh       # Release，生成 build/lulesh2.0
```

### 手动构建 — macOS Apple Silicon（OpenMP 后端）

```bash
cmake -S . -B build \
    -DCMAKE_PREFIX_PATH=/opt/homebrew/Cellar/kokkos/5.0.2 \
    -DOpenMP_CXX_FLAGS="-Xpreprocessor -fopenmp -I/opt/homebrew/opt/libomp/include" \
    -DOpenMP_CXX_LIB_NAMES="omp" \
    -DOpenMP_omp_LIBRARY=/opt/homebrew/opt/libomp/lib/libomp.dylib \
    -DCMAKE_BUILD_TYPE=Release

cmake --build build -j$(sysctl -n hw.ncpu)
```

### 手动构建 — Linux（OpenMP 后端）

```bash
cmake -S . -B build \
    -DCMAKE_PREFIX_PATH=/path/to/kokkos \
    -DCMAKE_BUILD_TYPE=Release

cmake --build build -j$(nproc)
```

### 手动构建 — Linux + NVIDIA GPU（CUDA 后端）

CUDA 后端要求 Kokkos 在安装时启用 `Kokkos_ENABLE_CUDA_LAMBDA=ON`，否则
`KOKKOS_LAMBDA` 宏不含 `__device__` 属性，nvcc 会拒绝在设备端调用该 lambda。

**推荐方式：使用一键脚本**（自动重建 Kokkos + 编译 LULESH）

```bash
./scripts/build-cuda.sh                          # A100（默认）
./scripts/build-cuda.sh --arch VOLTA70 --cuda-arch 70         # V100
./scripts/build-cuda.sh --help                   # 查看全部选项
```

**手动方式：**

```bash
# 步骤 1：以 CUDA_LAMBDA=ON 重建 Kokkos
cmake -S /path/to/kokkos/src -B /tmp/kokkos-build \
    -DCMAKE_INSTALL_PREFIX=/path/to/kokkos/install \
    -DKokkos_ENABLE_CUDA=ON \
    -DKokkos_ENABLE_CUDA_LAMBDA=ON \
    -DKokkos_ENABLE_OPENMP=ON \
    -DKokkos_ARCH_AMPERE80=ON \
    -DCMAKE_CXX_COMPILER=/path/to/kokkos/bin/nvcc_wrapper \
    -DCMAKE_BUILD_TYPE=Release
cmake --build /tmp/kokkos-build -j$(nproc)
cmake --install /tmp/kokkos-build

# 步骤 2：构建 LULESH
cmake -S . -B build-cuda \
    -DCMAKE_CXX_COMPILER=/path/to/kokkos/bin/nvcc_wrapper \
    -DCMAKE_PREFIX_PATH=/path/to/kokkos/install \
    -DKokkos_ENABLE_CUDA=ON \
    -DKokkos_ENABLE_CUDA_LAMBDA=ON \
    -DKokkos_ARCH_AMPERE80=ON \       # 按实际 GPU 架构修改，如 VOLTA70、HOPPER90
    -DCMAKE_BUILD_TYPE=Release

cmake --build build-cuda -j$(nproc)
```

`CMakeLists.txt` 中的 `kokkos_check` 块会自动检测 CUDA 后端；检测到
`nvcc_wrapper` 为 CXX 编译器时进入 Path A，所有 `.cc` 源文件保持 CXX 语言
（nvcc_wrapper 在内部重命名为 `.cu`），`--expt-extended-lambda` 通过 CXX
编译选项传入 nvcc_wrapper，从而避免 lambda 获得 `_INTERNAL_` 前缀。

---

## 运行

```bash
OMP_PROC_BIND=close ./build/lulesh2.0 [选项]
```

> **Apple Silicon 提示**：`OMP_PROC_BIND=close` 将线程绑定到相邻核心，避免
> P-core 与 E-core 混用导致的 barrier 不均衡（T=4→T=5 会出现约 22% 性能悬崖）。
> `scripts/run-openmp.sh` 在 macOS 上会自动将 `OMP_NUM_THREADS` 设为 P-core 数量。

| 选项 | 说明 | 默认值 |
|------|------|--------|
| `-s <N>` | 每个维度的网格边长（总单元数 = N³） | 30 |
| `-i <N>` | 最大迭代步数 | 9999999 |
| `-r <N>` | 区域数量 | 11 |
| `-b <N>` | 区域负载均衡系数 | 1 |
| `-c <N>` | 高代价区域的代价倍数 | 1 |
| `-p` | 打印每步进度 | 关 |
| `-q` | 静默模式 | 关 |
| `-h` | 显示帮助 | — |

### 正确性验证

```bash
OMP_PROC_BIND=close ./build/lulesh2.0 -s 10 -i 50
# 期望输出：Final Origin Energy = 8.104796e+04
```

或使用脚本：

```bash
./scripts/run-openmp.sh verify
```

### 性能基准

```bash
./scripts/run-openmp.sh bench   # 等价于 -s 45 -i 200
./scripts/run-openmp.sh scale   # 扩展性扫描（T=1~10）
```

---

## GPU 迁移状态

所有核函数均已完成面向 Kokkos 5 CUDA 后端的语法改造：

| 改造内容 | 状态 |
|---------|------|
| 所有核函数 `[&]` → `KOKKOS_LAMBDA`（`[=]`）+ `Kokkos::View` 提取 | ✓ |
| 几何辅助函数 `KOKKOS_INLINE_FUNCTION`（定义内联于头文件，无 `.cc`） | ✓ |
| `EosTemps` 缓冲区 `std::vector` → `Kokkos::View` | ✓ |
| `vnew`、`determ` 临时数组 → `Kokkos::View` | ✓ |
| Scatter buffer（`lulesh-stress.cc`）→ `Kokkos::View` | ✓ |
| `exit()` / `fprintf` → `Kokkos::abort` | ✓ |
| 设备端数学函数：`std::sqrt/fabs/cbrt/max` → `Kokkos::*` | ✓ |
| CMake 自动检测 CUDA 后端；nvcc_wrapper/g++ 双路径处理 | ✓ |
| OpenMP 后端正确性验证 | ✓ `8.104796e+04` |
| CUDA 后端实测（Linux + NVIDIA GPU） | 待完成 |

---

## 性能优化（OpenMP 后端，Apple M 系列）

通过消除 `Kokkos::parallel_for` / `parallel_reduce` 之间的同步屏障（barrier），
在 Apple M 系列（4 P-core，`-s 45 -i 200`）上实测：

| | 原版 LULESH 2.0 | 第一轮（Opt-1~3） | 第二轮（Opt-4~9） |
|---|---|---|---|
| 耗时 | 7.10 s | 3.65 s | **3.30 s** |
| FOM（z/s） | 2572 | 5498 | **5518** |
| 加速比 | 1.00× | 2.14× | **2.15×** |

### 主要优化内容

| 优化 | 位置 | 效果 |
|------|------|------|
| Opt-1：Hourglass 内核融合 | `lulesh-stress.cc` | −1 barrier，−6 全局临时向量 |
| Opt-2：EvalEOS 准备阶段 5→1 | `lulesh-eos.cc` | −4 barriers/rep/region |
| Opt-3：CalcEnergyForElems 11→1 | `lulesh-eos.cc` | −10 barriers/rep，−1 向量 |
| Opt-4：速度 + 位置更新融合 | `lulesh-nodal.cc` | −1 barrier |
| Opt-5：Courant + Hydro 双 reduce | `lulesh-timestep.cc` | −11 barriers/step |
| Opt-6：scatter + 声速计算融合 | `lulesh-eos.cc` | −11 barriers/step |
| Opt-7：ApplyMaterial 3→1 | `lulesh-eos.cc` | −2 barriers |
| Opt-8：InitStress 内联 | `lulesh-stress.cc` | −1 barrier，−3 向量 |
| Opt-9：运动学 + Lagrange 融合 | `lulesh-kinematics.cc` | −1 barrier |

---

## 清理

```bash
rm -rf build/
```
