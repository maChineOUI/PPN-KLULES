# LULESH 2.0 — Refactored Serial Build

LULESH（Livermore Unstructured Lagrangian Explicit Shock Hydrodynamics）是由
Lawrence Livermore National Laboratory 开发的 HPC 代理应用，用于评估处理器和编程
模型在拉格朗日流体力学计算上的性能。本版本在 LULESH 2.0 基础上进行了现代化重构。

原始项目：https://codesign.llnl.gov/lulesh.php

---

## 与原版的主要差异

| 项目 | 原版 LULESH 2.0 | 本版本 |
|------|----------------|--------|
| 构建系统 | Makefile | CMake 3.16+ |
| MPI | 支持 | 已移除（纯串行） |
| 可视化 | Silo/VisIt（可选） | 已移除 |
| 代码组织 | 4 个大文件 | 11 个单职责模块 |
| C++ 标准 | C++11 | C++20 |

---

## 项目结构

```
LULESH/
├── CMakeLists.txt
├── README
├── include/              # 头文件
│   ├── lulesh.h          # Domain 类、公共类型定义
│   ├── lulesh-geometry.h
│   ├── lulesh-stress.h
│   ├── lulesh-nodal.h
│   ├── lulesh-kinematics.h
│   ├── lulesh-viscosity.h
│   ├── lulesh-eos.h
│   ├── lulesh-timestep.h
│   ├── lulesh-integration.h
│   ├── lulesh-init.h
│   └── lulesh-util.h
├── src/                  # 源文件
│   ├── lulesh.cc         # main()
│   ├── lulesh-geometry.cc
│   ├── lulesh-stress.cc
│   ├── lulesh-nodal.cc
│   ├── lulesh-kinematics.cc
│   ├── lulesh-viscosity.cc
│   ├── lulesh-eos.cc
│   ├── lulesh-timestep.cc
│   ├── lulesh-integration.cc
│   ├── lulesh-init.cc
│   └── lulesh-util.cc
└── build/                # 构建产物（cmake 生成，不进版本控制）
```

---

## 模块说明

| 模块 | 功能 |
|------|------|
| `lulesh-geometry` | 形函数、体积/面积/特征长度计算 |
| `lulesh-stress` | 应力积分、沙漏（Hourglass/FB）控制 |
| `lulesh-nodal` | 力→加速度→速度→位置（节点运动学） |
| `lulesh-kinematics` | 速度梯度、变形率张量 |
| `lulesh-viscosity` | 单调人工黏性 Q 计算 |
| `lulesh-eos` | 物态方程（理想气体）、材料属性更新 |
| `lulesh-timestep` | 时间步长控制（Courant 与水力学约束） |
| `lulesh-integration` | Leap-Frog 时间积分主循环 |
| `lulesh-init` | 网格初始化、Domain 构造 |
| `lulesh-util` | 命令行解析、结果输出 |

---

## 构建

### 依赖

- CMake >= 3.16
- C++20 兼容编译器（Clang 13+ / GCC 11+）

### 编译

```bash
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build
```

可执行文件位于 `build/lulesh2.0`。

### 调试构建

```bash
cmake -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build
```

---

## 运行

```bash
./build/lulesh2.0 [选项]
```

| 选项 | 说明 | 默认值 |
|------|------|--------|
| `-s <N>` | 每个维度的网格边长 | 30 |
| `-i <N>` | 最大迭代步数 | 9999999 |
| `-r <N>` | 区域数量 | 11 |
| `-b <N>` | 区域负载均衡系数 | 1 |
| `-c <N>` | 高代价区域的代价倍数 | 1 |
| `-p` | 打印每步进度 | 关 |
| `-q` | 静默模式 | 关 |
| `-h` | 显示帮助 | — |

### 正确性验证

```bash
./build/lulesh2.0 -s 10 -i 50
# 期望输出：Final Origin Energy = 8.104796e+04
```

---

## 清理

```bash
rm -rf build/
```
